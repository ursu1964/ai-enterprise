# Policy Standard

Policies contain versioned metadata, scope, deterministic rules, precedence, bounded exceptions,
tests, approvers, effective dates, and a canonical hash. Deny is the default. Exceptions name an
owner and expiry and cannot override human-only authority. Decisions record inputs, policy versions,
reasons, correlation, and evidence. A policy change is reviewed as a public behavioral contract and
cannot silently broaden existing assignments.


name: ETRA Conformance

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install ETRA conformance dependencies
        run: python -m pip install "jsonschema>=4.26"
      - name: Validate Enterprise Technical Reference Architecture
        run: python tools/etra_conformance.py --root . --json
